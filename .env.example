# Optional analysis only. The offline pipeline never reads or requires this key.
OPENAI_API_KEY=
# Databricks SDK unified authentication; prefer a CLI profile or OAuth.
DATABRICKS_HOST=
DATABRICKS_CONFIG_PROFILE=DEFAULT
# Use a scoped token only when OAuth is unavailable. Never commit real values.
DATABRICKS_TOKEN=
