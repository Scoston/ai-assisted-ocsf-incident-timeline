# Copy to /etc/timeline/collectors/NAME.env with root-only permissions.
# Keep this initial boundary and case prefix stable for the lifetime of state.
INITIAL_START=2026-09-01T00:00:00Z
CASE_PREFIX=example-enterprise
# Supply short-lived source tokens through your approved credential delivery.
# Do not commit actual credentials. AWS/Databricks SDK auth may require custom
# service identity/environment setup; ProtectHome prevents home-profile access.
