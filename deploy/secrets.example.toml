# Copy outside the repository and populate from your secret manager.
[auth]
redirect_uri = "https://timeline.example.test/oauth2callback"
cookie_secret = "REPLACE_WITH_STRONG_RANDOM_SECRET"
client_id = "OIDC_APPLICATION_ID"
client_secret = "SECRET_MANAGER_VALUE"
server_metadata_url = "https://login.microsoftonline.com/TENANT_ID/v2.0/.well-known/openid-configuration"
# Do not expose identity or access tokens to the app.
