[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "forensic-timeline-ai-demo"
version = "0.4.0"
requires-python = ">=3.10"
description = "Breach timeline demo with parsers, IOC extraction, TI/AI enrichment, SaaS/AI/DFIR catalogs, and Streamlit UI"
